server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fooddb?verifyServerCertificate=false&useSSL=false&requireSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  profiles:
    active: mysql
